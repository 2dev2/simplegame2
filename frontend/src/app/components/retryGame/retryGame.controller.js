(function() {
  'use strict';

  angular
    .module('frontend')
    .controller('retryGameController', retryGameController);

  /** @ngInject */
  function retryGameController($scope,$timeout, webDevTec,randomNumberGeneration,generateListOfObject,$state) {
     var vm = this;
  }

})();
